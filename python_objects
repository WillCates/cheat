#python variables objects read input namespaces

#User input:
x = input("Enter name")

#Variable namespace override
# Use rule LEGB (Local, Enclosing, Global, Builtin)
nonlocal a = 10
global b = 11
__builtins__.VARNAME = something

#Show Variables in namespace
globals()
locals()

#if you import a library, you can check the library objects:
dir(LIBRARY)

#object types:
 - Literals
 - Keywords
 - Operators
 - Delimiters
 - Comments
 - Variables
