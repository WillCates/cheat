#python functions arguments

#default / optional variable 'c'
def func(a, b, c=10)
#Calling function
func(a=2, b=4, c=11)

#Forced function type
def func(alpha:int,bravo:float)
#Syntactical type suggestion (not enforced)
func(alpha:int,bravo:float)->str:

#Returning & Assigning multiple outputs from function:
def somefunc():
  return x, y
a,b = somefunc()

#Can overwrite entire python functions via assignment (may want to save old function or restart the python process/namespace to fix)
x = print, print = 100
